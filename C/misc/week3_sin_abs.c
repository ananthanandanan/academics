
#include <stdio.h>

/*
#include <math.h> /* has  sin(), abs(), and fabs() 
int main(void)
{ 
double interval;
int i;
for(i = 0; i <30; i++)
{
 interval = i/10.0;
 printf("sin( %lf ) = %lf \t\n", interval, fabs(sin(interval))); // changed abs() to fabs because fabs returns absolute value as double
}


printf("\n+++++++\n");
return 0;
}

*/

int main()
{
    int n, i=1,j=2;
    n = i,j+1;
    printf("%d",n);
    return 0;
}
